{% extends 'base.html.twig' %}

{% block title %}Hello ReservationController!{% endblock %}

{% block body %}


	<div class='container'>
	
		<h1 class='mt-4'>Réservation</h1>

		<form name="reservation" method="post" enctype="multipart/form-data" class="row">

  
      <div class="form-group col">
        {{ form_label(form.user) }}
        {{ form_widget(form.user) }}
      </div>
      <div class="form-group col">
        {{ form_label(form.nb_covers) }}
        {{ form_widget(form.nb_covers) }}
      </div>
          <div class="form-group col">
        {{ form_label(form.planning) }}
        {{ form_widget(form.planning) }}
        <div class="form-error">
         {{ form_errors(form.planning) }}
       </div>
      </div>
      <div class="form-btn-group col">
        {{ form_label(form.hours) }}
        {{ form_widget(form.hours) }}
        <div class="form-error">
         {{ form_errors(form.hours) }}
       </div>
      </div>
     {{ form_row(form._token) }}
      <button type="submit" id="reservation_submit" name="reservation[submit]" class="btn btn-info mt-4 col">Reserver</button>

    </form>
	</div>
{% endblock %}
